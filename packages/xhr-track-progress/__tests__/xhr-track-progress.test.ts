import xhrTrackProgress from '../src/xhrTrackProgress';

describe('@building-block/xhr-track-progress', () => {
  it('should pass sanity check', () => {
    let x= true;

    expect(x).toBe(true);
  });
});

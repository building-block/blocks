{
  "name": "blocks",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "dev": "lerna run dev",
    "clean": "git clean -dfiX ."
  },
  "devDependencies": {
    "lerna": "^3.15.0",
    "microbundle": "^0.11.0"
  }
}

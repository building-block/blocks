{
  "name": "blocks",
  "private": true,
  "scripts": {
    "prebuild": "lerna bootstrap",
    "build": "lerna run build",
    "dev": "lerna run dev",
    "test": "jest",
    "pretest": "npm run lint",
    "clean": "git clean -dfiX ."
  },
  "devDependencies": {
    "lerna": "^3.15.0"
  }
}
